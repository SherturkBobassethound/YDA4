# Core FastAPI and server
fastapi==0.115.11
uvicorn==0.34.0
pydantic==2.10.6
python-dotenv==1.0.1
python-multipart==0.0.6

# NumPy version constraint to avoid compatibility issues
numpy<2

# PyTorch - required by Whisper (CPU version will be auto-selected if no GPU)
torch
openai-whisper

# LangChain for embeddings and text splitting
langchain==0.3.21
langchain-huggingface
langchain-community

# Embeddings (sentence-transformers is a dependency of langchain-huggingface)
# No need to specify separately

# Web scraping and processing
beautifulsoup4==4.13.3
feedparser==6.0.11
requests==2.32.3

# Document processing
yt-dlp
youtube-transcript-api==0.6.3

# Authentication
python-jose[cryptography]==3.3.0
supabase==2.12.0